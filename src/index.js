import validator from './validator.js';
let button = document.getElementById ("button")
button.addEventListener ("click", function (validCard) {
let cardNumber= document.getElementById (cardNumber).value;
}
console.log(validator);
